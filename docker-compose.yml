version: '3.8'
services:
  auth:
    image: ziggybaba/php-api:latest
    build:
      context: .
      dockerfile: ./Dockerfile
      target: development
    ports:
      - '8000:8000'
    volumes:
      - .:/usr/src/app